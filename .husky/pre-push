# Ensure no uncommitted changes are present
if ! git diff-index --quiet HEAD --; then
  echo "You have uncommitted changes. Please commit or stash them before pushing."
  exit 1
fi

echo "No uncommitted changes. Proceeding with push..."